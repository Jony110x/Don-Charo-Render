services:
  - type: web
    name: don-charo-backend
    env: python
    plan: free
    branch: main
    rootDir: backend
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
